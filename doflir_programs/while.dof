define main {
    A -> int;
    B -> int;
    C -> int;
    D -> int;
    B = 493;
    D = 2;
    C = 32;
    A = (1 + (2 * 2) ^ 2) ^ 3;
    while (A + B > D) {
        if (A > B) {
            A = A // 4;
            B = B + D;
        }
        else {
            C = A + B;
        }
        D = D + 87;
    }
    D = (B + A * C) * D;
}