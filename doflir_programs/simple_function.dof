test -> float;
test = 2.2;


define times_two -> float (num -> float) {
	return num * 2.0;
}

define main {
	eight -> float;
	result -> float;
	eight = 8.0;
	result = times_two(eight);
	print("This should be 16: ");
	print(result);
}
