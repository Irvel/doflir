define two -> float () {
	return 8.0;
}

define one -> float (pi -> float, tau -> float) {
	return 2.0 * pi + tau;
}

define three -> float (esp -> float, cop -> float) {
	cop = 1.344 + 2;
	return 2.0 * esp + cop;
}

define main {
	test -> float;
	test2 -> float;
	print("test");
	test = one(two(), two());
	test2 = three(two(), 9.21);
}